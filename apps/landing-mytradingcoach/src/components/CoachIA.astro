---
---

<section class="coach-section" id="coach">
  <div class="coach-wrap">

    <!-- Header -->
    <div class="reveal" style="text-align:center;margin-bottom:56px">
      <span class="sec-label">Coach IA Personnalisé</span>
      <h2>Une IA qui te connaît.<br /><em>Vraiment.</em></h2>
      <p class="sec-sub" style="max-width:540px;margin:0 auto">
        Tous les journaux de trading analysent tes stats.
        MyTradingCoach est le seul à analyser tes stats
        <strong>en connaissant ta stratégie</strong>.
        C'est la différence entre un robot et un coach.
      </p>
    </div>

    <!-- Comparaison avant/après -->
    <div class="coach-compare reveal">
      <div class="coach-col bad">
        <div class="coach-col-label">
          <span class="coach-col-ic bad-ic">✗</span>
          Analyse générique
        </div>
        <div class="coach-bubble bad-bubble">
          <div class="coach-avatar bad-av">🤖</div>
          <div class="coach-msg">
            <p>
              "Attention — tu as pris <strong>18 trades aujourd'hui</strong>.
              C'est élevé et peut indiquer du overtrading.
              Essaie de limiter tes entrées et d'être plus sélectif."
            </p>
            <span class="coach-tag bad-tag">Conseil inutile</span>
          </div>
        </div>
        <p class="coach-col-note">
          Sans contexte, l'IA confond ta stratégie avec un problème.
          18 trades/jour c'est peut-être ta norme.
        </p>
      </div>

      <div class="coach-divider">
        <div class="coach-divider-line"></div>
        <div class="coach-divider-vs">VS</div>
        <div class="coach-divider-line"></div>
      </div>

      <div class="coach-col good">
        <div class="coach-col-label">
          <span class="coach-col-ic good-ic">✓</span>
          Coach personnalisé
        </div>
        <div class="coach-bubble good-bubble">
          <div class="coach-avatar good-av">🎯</div>
          <div class="coach-msg">
            <p>
              "Tes <strong>18 trades aujourd'hui</strong> sont dans ta norme
              de scalper ICT. Ce qui attire l'attention :
              <strong>6 de tes 7 pertes</strong> sont survenues entre 15h-16h NY,
              sur des OB que le price avait déjà visités.
              Évite les re-entrées sur OB invalidés après le London close."
            </p>
            <span class="coach-tag good-tag">Conseil actionnable</span>
          </div>
        </div>
        <p class="coach-col-note">
          L'IA connaît ton profil. Elle analyse dans le bon contexte
          et détecte ce qui compte vraiment.
        </p>
      </div>
    </div>

    <!-- 3 étapes -->
    <div class="coach-steps reveal">
      <div class="coach-step">
        <div class="coach-step-num">01</div>
        <div class="coach-step-content">
          <h3>Tu définis ton profil</h3>
          <p>
            Style de trading, stratégie (ICT, SMC, Price Action, Algo…),
            sessions préférées (London, NY, Asian), fréquence normale de trades.
            5 minutes. Une fois.
          </p>
          <div class="coach-step-tags">
            <span>⚡ Scalping</span>
            <span>📅 Day Trading</span>
            <span>🌊 Swing</span>
            <span>ICT</span>
            <span>SMC</span>
            <span>Order Blocks</span>
            <span>FVG</span>
          </div>
        </div>
      </div>

      <div class="coach-step-arrow">→</div>

      <div class="coach-step">
        <div class="coach-step-num">02</div>
        <div class="coach-step-content">
          <h3>Tu journalises tes trades</h3>
          <p>
            En 30 secondes par trade. L'IA connaît déjà ton contexte —
            elle sait comment interpréter chaque entrée, chaque perte,
            chaque série.
          </p>
        </div>
      </div>

      <div class="coach-step-arrow">→</div>

      <div class="coach-step">
        <div class="coach-step-num">03</div>
        <div class="coach-step-content">
          <h3>L'IA t'analyse vraiment</h3>
          <p>
            Weekly debrief, insights quotidiens, objectifs de la semaine —
            tout est adapté à <em>ta</em> stratégie. Pas à un trader fictif.
          </p>
        </div>
      </div>
    </div>

    <!-- Citation -->
    <div class="coach-quote reveal">
      <div class="coach-quote-inner">
        <div class="coach-quote-mark">"</div>
        <blockquote>
          J'ai importé mon mois de mai. L'IA interprétait des choses qui
          ne correspondaient pas à ma réalité — elle ne connaissait pas ma strat.
          Depuis que j'ai renseigné mon profil ICT scalper, les analyses
          sont complètement différentes. Elle me parle de mes vraies erreurs.
        </blockquote>
        <div class="coach-quote-author">
          <div class="coach-quote-av">V</div>
          <div>
            <div class="coach-quote-name">Val</div>
            <div class="coach-quote-role">Scalper ICT · Futures CME</div>
          </div>
        </div>
      </div>
    </div>

    <!-- CTA -->
    <div class="coach-cta reveal">
      <a href="https://app.mytradingcoach.app/register" class="btn-p">
        Créer mon profil trader gratuitement
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none"
          stroke="currentColor" stroke-width="2.5">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
      <p class="coach-cta-note">Aucune CB requise · 7 jours Premium offerts</p>
    </div>

  </div>
</section>

<style>
  .coach-section {
    padding: 100px 5%;
    position: relative;
    overflow: hidden;
  }

  .coach-section::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    background: radial-gradient(ellipse at 50% 0%, rgba(59,130,246,.06) 0%, transparent 60%);
    pointer-events: none;
  }

  .coach-wrap { max-width: 1000px; margin: 0 auto; }

  /* Comparaison */
  .coach-compare {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 24px;
    align-items: start;
    margin-bottom: 72px;
  }

  .coach-col { display: flex; flex-direction: column; gap: 16px; }

  .coach-col-label {
    display: flex; align-items: center; gap: 8px;
    font-size: 13px; font-weight: 600; color: var(--txt2);
    font-family: var(--mono); letter-spacing: .3px;
  }

  .coach-col-ic {
    width: 22px; height: 22px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 12px; font-weight: 700; flex-shrink: 0;
  }
  .bad-ic  { background: rgba(239,68,68,.15); color: #ef4444; }
  .good-ic { background: rgba(16,185,129,.15); color: #10b981; }

  .coach-bubble {
    border-radius: 14px; padding: 18px;
    display: flex; gap: 14px; align-items: flex-start;
  }
  .bad-bubble  { background: rgba(239,68,68,.05); border: 1px solid rgba(239,68,68,.15); }
  .good-bubble { background: rgba(16,185,129,.05); border: 1px solid rgba(16,185,129,.2); }

  .coach-avatar {
    width: 36px; height: 36px; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 18px; flex-shrink: 0;
  }
  .bad-av  { background: rgba(239,68,68,.1); }
  .good-av { background: rgba(16,185,129,.1); }

  .coach-msg { flex: 1; }
  .coach-msg p { font-size: 13px; line-height: 1.6; color: var(--txt2); margin-bottom: 10px; }
  .coach-msg p strong { color: var(--txt); }

  .coach-tag {
    font-size: 10px; font-family: var(--mono); padding: 2px 8px;
    border-radius: 100px; font-weight: 600;
  }
  .bad-tag  { background: rgba(239,68,68,.1); color: #ef4444; }
  .good-tag { background: rgba(16,185,129,.1); color: #10b981; }

  .coach-col-note {
    font-size: 12px; color: var(--txt3); line-height: 1.5;
    font-style: italic; padding: 0 4px;
  }

  .coach-divider {
    display: flex; flex-direction: column;
    align-items: center; gap: 12px; padding-top: 48px;
  }
  .coach-divider-line {
    width: 1px; height: 60px;
    background: linear-gradient(180deg, transparent, var(--b2), transparent);
  }
  .coach-divider-vs {
    font-size: 11px; font-family: var(--mono); color: var(--txt3);
    background: var(--bg3); border: 1px solid var(--b2);
    padding: 4px 8px; border-radius: 6px; letter-spacing: 1px;
  }

  /* 3 étapes */
  .coach-steps {
    display: grid;
    grid-template-columns: 1fr auto 1fr auto 1fr;
    gap: 16px;
    align-items: start;
    margin-bottom: 72px;
    background: var(--card);
    border: 1px solid var(--b);
    border-radius: 16px;
    padding: 32px;
  }

  .coach-step { display: flex; flex-direction: column; gap: 12px; }

  .coach-step-num {
    font-size: 11px; font-family: var(--mono); font-weight: 700;
    color: var(--blue2); letter-spacing: 1px;
    background: rgba(59,130,246,.1); border: 1px solid rgba(59,130,246,.2);
    padding: 2px 8px; border-radius: 6px; align-self: flex-start;
  }

  .coach-step-content h3 {
    font-family: var(--disp); font-size: 16px; font-weight: 600;
    margin-bottom: 8px; color: var(--txt); letter-spacing: -.2px;
  }
  .coach-step-content p { font-size: 13px; color: var(--txt2); line-height: 1.6; }

  .coach-step-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 8px; }
  .coach-step-tags span {
    font-size: 11px; font-family: var(--mono);
    background: var(--bg3); border: 1px solid var(--b);
    padding: 3px 8px; border-radius: 100px; color: var(--txt2);
  }

  .coach-step-arrow {
    color: var(--blue2); font-size: 20px;
    padding-top: 40px; flex-shrink: 0;
  }

  /* Citation */
  .coach-quote { margin-bottom: 56px; }

  .coach-quote-inner {
    background: var(--card); border: 1px solid var(--b);
    border-left: 3px solid var(--blue2);
    border-radius: 14px; padding: 28px 32px;
    max-width: 680px; margin: 0 auto;
  }

  .coach-quote-mark {
    font-family: Georgia, serif; font-size: 60px;
    color: var(--blue2); opacity: .3;
    line-height: .6; margin-bottom: 12px;
  }

  blockquote {
    font-size: 15px; color: var(--txt2);
    line-height: 1.7; font-style: italic; margin-bottom: 20px;
  }

  .coach-quote-author { display: flex; align-items: center; gap: 12px; }

  .coach-quote-av {
    width: 36px; height: 36px; border-radius: 50%;
    background: linear-gradient(135deg, var(--blue), var(--pulse));
    display: flex; align-items: center; justify-content: center;
    font-size: 14px; font-weight: 700; color: #000;
    font-family: var(--mono); flex-shrink: 0;
  }

  .coach-quote-name { font-size: 13px; font-weight: 600; color: var(--txt); }
  .coach-quote-role { font-size: 11px; font-family: var(--mono); color: var(--txt3); }

  /* CTA */
  .coach-cta { text-align: center; }

  .btn-p {
    background: var(--blue); color: #fff; padding: 14px 28px;
    border-radius: 10px; font-size: 15px; font-weight: 600;
    text-decoration: none; display: inline-flex; align-items: center; gap: 8px;
    box-shadow: 0 0 32px rgba(59,130,246,.4);
    transition: all .2s; font-family: var(--disp);
  }
  .btn-p:hover { background: var(--blue2); transform: translateY(-2px); }

  .coach-cta-note {
    margin-top: 12px; font-size: 12px;
    color: var(--txt3); font-family: var(--mono);
  }

  /* Mobile */
  @media (max-width: 767px) {
    .coach-section { padding: 60px 16px; }

    .coach-compare { grid-template-columns: 1fr; gap: 16px; }
    .coach-divider { flex-direction: row; padding-top: 0; }
    .coach-divider-line { width: 60px; height: 1px; }

    .coach-steps {
      grid-template-columns: 1fr;
      padding: 20px;
    }
    .coach-step-arrow {
      padding-top: 0;
      transform: rotate(90deg);
      align-self: center;
    }

    blockquote { font-size: 13px; }
    .coach-quote-inner { padding: 20px; }
  }
</style>
