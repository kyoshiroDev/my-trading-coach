# .github/workflows/auto-merge.yml
name: Auto merge dependabot
on: pull_request

jobs:
  automerge:
    if: github.actor == 'dependabot[bot]'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto merge ready"
